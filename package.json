{
  "name": "components",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "yarn workscpaces run build",
    "serve": "wsrun --parallel serve",
    "start": "concurrently \"wsrun --parallel start\" \"wsrun --parallel serve\""
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "concurrently": "^6.2.1",
    "wsrun": "^5.2.4"
  }
}